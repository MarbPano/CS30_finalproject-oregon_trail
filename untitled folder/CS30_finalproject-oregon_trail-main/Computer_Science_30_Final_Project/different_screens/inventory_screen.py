import pygame
import sys
# import window_creation as wc
# import shop_screen as ss

#-------#
#Showing the inventory
#this code is so user has the option to show the inventory and what they have
black = (0,0,0)
pygame.init()

res = (720,720)

screen = pygame.display.set_mode(size=(res))
screen.fill(black)
